export type NavLink = {
  name: string;
  to: string;
};
export type devis = {
  nom: string;
  prenom: string;
  tel: string;
  email: string;
  adresse: string;
  clientType: string;
  message: string;
};
